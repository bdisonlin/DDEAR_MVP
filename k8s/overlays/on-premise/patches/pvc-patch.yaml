apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data-service-pvc
spec:
  storageClassName: nfs-client   # nfs-subdir-external-provisioner StorageClass
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 50Gi
