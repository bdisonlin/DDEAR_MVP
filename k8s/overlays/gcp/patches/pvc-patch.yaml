apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data-service-pvc
spec:
  storageClassName: filestore-rwx   # GCP Filestore CSI StorageClass (ReadWriteMany)
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 50Gi
